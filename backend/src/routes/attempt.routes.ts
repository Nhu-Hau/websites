// import { Router } from "express";
// import { submitAttempt, latestAnalytics } from "../controllers/testsPlay.controller";
// // nếu bạn muốn bắt buộc đăng nhập:
// // import requireAuth from "../middleware/requireAuth";

// const router = Router();

// router.post("/", /* requireAuth, */ submitAttempt);
// router.get("/latest/:userId", /* requireAuth, */ latestAnalytics);

// export default router;